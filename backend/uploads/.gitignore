# Ignore all files in uploads directory (user uploaded files)
*

# But keep the .gitignore file itself
!.gitignore

# Keep the directory structure (transactions folder)
!transactions/
transactions/*
!transactions/.gitkeep

